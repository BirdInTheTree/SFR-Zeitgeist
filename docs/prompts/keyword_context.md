These keywords already exist from yesterday and earlier broadcasts today:
{keywords}
If a segment covers the same story, reuse the EXACT keyword. Only create a new keyword if it's a genuinely new topic.
